const divide = () => {
    let answer = 2000 / 100
    return answer
}

const square = (number) => {
 let sum = 2 * number
 return sum
}

const add = (num1, num2) => {
    let sum = num1 + num2
    return sum
}